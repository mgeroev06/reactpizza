import React, {FC} from 'react';
import NotFoundBlock from "../components/NotFound";

const NotFound: FC = () => {
    return (
      <div>
        <NotFoundBlock/>
      </div>
    );
};

export default NotFound;